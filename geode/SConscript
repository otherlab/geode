Import('env library')

env = env.Clone(use_libpng=1,use_libjpeg=1,use_openexr=1,use_openmesh=1,use_gmp=1)

# Minimal dependencies:
# env = env.Clone(use_libpng=0,use_libjpeg=0,use_openexr=0,use_openmesh=0,use_python=0)

library(env,'geode',pyname='geode_wrap')
